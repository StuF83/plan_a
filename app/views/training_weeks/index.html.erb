<h1>Training weeks index page</h1>
<div class="grid">
  <% @training_weeks.each do |training_week| %>
    <div>
        <p><%= link_to "#{training_week.start_date}", training_week %></p>
        <p><%= link_to "Destroy", training_week, data: {turbo_method: :delete} %></p>
    </div>
  <% end %>
</div>
